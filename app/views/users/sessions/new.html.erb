<div>
	<a class="hiddenanchor" id="signin"></a>
	<div class="login_wrapper">
		<div class="animate form login_form">
			<section class="login_content">
				<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
					<h1>Login Form</h1>
					<div>
						<%= f.email_field :email, autofocus: true, placeholder: true, class: "form-control" %>
					</div>
					<div>
						<%= f.password_field :password, placeholder: true, autocomplete: "current-password", class:"form-control" %>
					</div>
					<div>
						<%= f.submit "Log in", class:"btn btn-primary submit" %>
					</div>
					<%= render "users/shared/links" %>
				<% end %>
			</section>
		</div>
	</div>
</div>
